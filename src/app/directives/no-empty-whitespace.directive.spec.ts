import { NoEmptyWhitespaceDirective } from './no-empty-whitespace.directive';

describe('NoEmptyWhitespaceDirective', () => {
  it('should create an instance', () => {
    const directive = new NoEmptyWhitespaceDirective();
    expect(directive).toBeTruthy();
  });
});
